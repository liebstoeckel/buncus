node_modules/
*.sqlite
*.sqlite-*
dist/
.DS_Store
.idea/

# GitHub App private keys — never commit. Belong in env/secret stores only.
*.pem
*.key

# Claude Code session state and git worktrees (.claude/worktrees/*).
.claude/

# Reference clone of giscus/giscus — kept locally for comparison, not part of buncus.
giscus-eval/