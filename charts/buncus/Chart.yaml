apiVersion: v2
name: buncus
description: Self-hosted, themeable host for GitHub Discussions comments. A Bun-native giscus reimplementation.
type: application
home: https://github.com/liebstoeckel/buncus
sources:
  - https://github.com/liebstoeckel/buncus
maintainers:
  - name: Leon Kaucher
    email: contact@liebstoeckel.app
kubeVersion: ">=1.23.0-0"
# These are a development baseline and may lag the latest release. The published
# chart is always pinned to the release tag: the publish-chart workflow runs
# `helm package --version <tag> --app-version <tag>`, so pulling a given chart
# version resolves to the matching image. (release-please cannot bump this file
# automatically: its extra-files updater rejects paths outside the package dir.)
version: 0.1.2
appVersion: "0.1.2"
